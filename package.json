{
  "name": "fuel-pass-bangladesh",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "client": "npm run dev --workspace=client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install"
  },
  "devDependencies": {
    "@types/react": "~19.2.10",
    "concurrently": "^9.1.2"
  }
}
